{{- $server   := false -}}{{/* true if server */}}
{{- $mobile   := false -}}{{/* true if mobile */}}
{{- $termux   := false -}}{{/* true if termux */}}
{{- "" -}}

{{- if      eq .chezmoi.hostname "buratino" -}}
{{-   $server = true -}}
{{- else if eq (env "OSTYPE") "linux-android" -}}
{{-   $mobile = true -}}
{{-   $termux = true -}}
{{- end -}}

[data]
    # Autogenerated to derive variables, do not edit.
    server = {{ $server }}
    mobile = {{ $mobile }}
    termux = {{ $termux }}
