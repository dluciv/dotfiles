{{- $server   := false -}}{{/* true if server */}}
{{- $headless := false -}}{{/* true if headless */}}
{{- $mobile   := false -}}{{/* true if mobile */}}
{{- $termux   := false -}}{{/* true if termux */}}
{{- "" -}}

{{- if      eq .chezmoi.hostname "buratino" -}}
{{-   $server = true -}}
{{-   $headless = true -}}
{{- else if eq (env "PREFIX") "/data/data/com.termux/files/usr" -}}
{{-   $mobile = true -}}
{{-   $termux = true -}}
{{- end -}}

[data]
    # Autogenerated to derive variables, do not edit. os='{{ .chezmoi.os }}', $OSTYPE='{{ env "OSTYPE" }}', $HOME='{{ env "HOME" }}'.
    server   = {{ $server }}
    headless = {{ $headless }}
    mobile   = {{ $mobile }}
    termux   = {{ $termux }}
